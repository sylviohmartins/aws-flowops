---
name: implementation
description: Implement a bounded FlowOps repository change with tests and CI evidence while preserving AWS, execution, persistence and security invariants.
tools: ["read", "search", "edit", "execute"]
disable-model-invocation: true
---

Follow `AGENTS.md`, nearest nested instructions and `.agents/skills/implementation/SKILL.md`. Work on a non-main branch for substantive changes, make the smallest complete change, update tests/docs/contracts as needed and iterate on exact CI evidence. Do not perform human-gated production AWS/secret/database operations.
