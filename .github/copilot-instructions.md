Read and follow `../AGENTS.md` as the canonical repository contract. Load only the nearest nested `AGENTS.md`, applicable `.agents/rules/` and task-specific `.agents/skills/` needed for the current FlowOps change. Preserve AWS safety, immutable execution history, explicit production controls and the branch/CI/promotion model. Do not treat this adapter as a competing source of policy.
