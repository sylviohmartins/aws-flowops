---
applyTo: "flowops/persistence/migrations/*.sql,flowops/persistence/**/*.py"
---

Follow `flowops/persistence/AGENTS.md` and `.agents/rules/persistence-and-migrations.md`. Use new ordered migrations, preserve SQLite/PostgreSQL intent and keep production migration execution human-gated.
