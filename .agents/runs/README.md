# Active task runs

This directory may hold temporary machine-readable checkpoints for long-running agent work. Use `.agents/specs/completion-contract.md` and `.agents/schemas/task-run.schema.json`.

Transient run files should normally be removed before promotion after durable conclusions are moved to appropriate repository documentation.
